{
  "scripts": {
    "build": "npm-run-all build:*",
    "build:html": "resumed --output public/index.html && cp public/index.html public/404.html",
    "format": "prettier --ignore-path .gitignore --write .",
    "test": "resumed validate"
  },
  "dependencies": {
    "jsonresume-theme-elegant": "^1.16.1",
    "resumed": "^1.0.3"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^2.6.2"
  }
}
